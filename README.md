# Chrome 语音输入转文字 Web 应用

一个基于 Chrome Web Speech API 的前端示例：采集麦克风语音并实时转换成文字。

## 使用方式

1. 直接在 Chrome 中打开 `index.html`（推荐使用 localhost 或 HTTPS）。
2. 选择识别语言后点击“开始采集”。
3. 说话后可在文本框查看实时转写结果。
4. 可使用“复制文字”和“清空结果”按钮进行操作。

## 注意事项

- 建议使用最新版 Google Chrome。
- 首次使用需要允许麦克风权限。
- Web Speech API 在不同系统上的识别效果会有差异。
